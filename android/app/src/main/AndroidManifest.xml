<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- ⚠ САМО за сваляне на езиковите пакети на Библията
         (lib/bible_packs.dart). Приложението иначе чете изцяло офлайн — от
         базите в assets/ — и не праща нищо навън. Външните препратки към
         azbyka.ru се отварят в БРАУЗЪРА (external_link.dart), което не иска
         разрешение. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
    </queries>
    <application
        android:label="@string/app_name"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- ⚠ ANDROID APP LINKS — линкове към цитати, споделяни навън.
                 Отварят се ПРАВО в приложението, без да питат.

                 Работи само защото ДВЕТЕ страни се сочат взаимно:
                 тук приложението заявява претенция за адресите, а домейнът
                 я потвърждава в
                 https://isihiabg.github.io/.well-known/assetlinks.json
                 Без второто всяко приложение би могло да прихваща чужди
                 линкове.

                 ⚠⚠ ФАЙЛЪТ СТОИ В КОРЕНА НА ДОМЕЙНА, не в папката на проекта.
                 Android го чете САМО от `https://<хост>/.well-known/`,
                 независимо накъде сочи линкът. Затова той живее в ОТДЕЛНО
                 хранилище — `isihiaBG/isihiaBG.github.io` — а не в
                 `orthodox_calendar/docs/`. Първият опит беше в подпапка и
                 валидаторът на Google отказа с ERROR_CODE_FETCH_ERROR.

                 ⚠ `autoVerify="true"` е ключът: при инсталиране Android САМ
                 отива на домейна и сравнява package name и SHA-256 на
                 подписващия ключ. Не съвпаднат ли — линкът тихо се отваря в
                 браузъра, без грешка. Затова и двете бяха закотвени преди
                 тази стъпка: смяната им после чупи всеки вече споделен линк.

                 ⚠ `pathPrefix` е стеснен до /q — НЕ целия домейн. Иначе
                 приложението би прихващало и обикновените страници на сайта
                 (напр. самото README), които трябва да се отварят в браузър. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="https"
                      android:host="isihiabg.github.io"
                      android:pathPrefix="/q"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
