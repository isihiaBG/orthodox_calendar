import 'package:flutter/material.dart';

class AppColors {
  // AppBar
  static const appBarWeekday = Color(0xFF2C3B4D);  // индиго
  static const appBarSunday  = Color(0xFF7b002c);  // винено червено

  // Фонове
  static const background        = Color(0xFF1E1510);
  static const backgroundCard    = Color(0xFF2A1F14);
  static const toolbar           = Color(0xFF1A1A1A);  // почти черен

  static const sectionTitle      = Color(0xFF8A9BB0);  // синкаво сиво
  static const sectionDivider    = Color(0xFF2A2A2A);  // тъмна линия
  
  static const drawerBackground  = Color(0xFF1A1A1A);
  static const drawerIcon        = Color(0xFF8A8A8A);
  static const drawerDivider     = Color(0xFF2A2A2A);

  // Текст
  static const textPrimary       = Colors.white;
  static const textSecondary     = Colors.white70;
  static const textMuted         = Colors.white54;
  static const fastText          = Colors.white70;
  static const sectionTitleSunday= Color.fromARGB(255, 179, 127, 145);  // бордо нюанс


  // Знаци на светии
  static const signRedHex        = '#CC0000';
  static const signRed           = Color.fromARGB(255, 235, 152, 182); //(0xFFCC0000);
  static const signWhite         = Colors.white;
}
